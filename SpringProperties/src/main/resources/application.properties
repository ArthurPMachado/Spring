info.app.name=Spring Boot with Actuator
info.app.description=A project to test the spring actuator and your endpoints
info.app.version=1.0.0

# Use wildcard "*" to expose all endpoints
# Can also expose individual endpoints with a comma-delimited list
management.endpoints.web.exposure.include=*

# Exclude individual endpoints with a comma-delimited list
# management.endpoints.web.exposure.exclude=health, info

# Define properties
coach.name=Arthur Machado
team.name=The Machado Club

# Change Spring Boot embedded server port
server.port=7070

# Set the context path of the application
# All request should be prefixed with /api
server.servlet.context-path=/api